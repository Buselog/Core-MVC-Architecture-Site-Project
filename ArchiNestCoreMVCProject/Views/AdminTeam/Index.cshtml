@model List<ArchiNestCoreMVCProject.Entities.Team>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}

<div class="col-lg-12 grid-margin stretch-card">
	<div class="card">
		<div class="card-body">
			<h4 class="card-title">Team</h4>
			<div class="table-responsive">
				<table class="table table-striped table-hover">
					<thead>
						<tr>
							<th>#</th>
							<th>Name</th>
							<th>Field</th>
							<th>Image URL</th>
							<th>Operations</th>
						</tr>
					</thead>
					<tbody>
						@foreach (var item in Model)
						{
							<tr>
								<td>@item.TeamId</td>
								<td>@item.Name</td>
								<td>@item.Field.FieldName</td>
								<td align="center"><img src="@item.ImageUrl" style="height:61px; width:69px;" /></td>
								<td>
									<a type="button" class="btn btn-inverse-primary btn-fw" href="/AdminTeam/Update/@item.TeamId">Update</a>
									&nbsp;
									<a type="button" class="btn btn-inverse-danger btn-fw" href="/AdminTeam/Delete/@item.TeamId">Delete</a>
								</td>
							</tr>
						}

					</tbody>
				</table>
			</div>
		</div>
	</div>
</div>


