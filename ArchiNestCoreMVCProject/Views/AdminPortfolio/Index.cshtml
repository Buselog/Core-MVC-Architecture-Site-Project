@model List<ArchiNestCoreMVCProject.Entities.Portfolio>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}

<div class="col-lg-12 grid-margin stretch-card">
	<div class="card">
		<div class="card-body">
			<h4 class="card-title">Portfolio</h4>
			<div class="table-responsive">
				<table class="table table-striped table-hover">
					<thead>
						<tr>
							<th>#</th>
							<th>Title</th>
							<th>Full Title</th>
							<th>Description</th>
							<th>Image URL</th>
							<th>Operations</th>
						</tr>
					</thead>
					<tbody>
						@foreach (var item in Model)
						{
							<tr>
								<td>@item.PortfolioId</td>
								<td>@item.Title</td>
								<td>@item.FullTitle</td>
								<td>@item.Description.Substring(0, 20)...</td>
								<td align="center"><img src="@item.ImageUrl" style="height:61px; width:69px;" /></td>
								<td>
									<a type="button" class="btn btn-inverse-primary btn-fw" href="/AdminPortfolio/Update/@item.PortfolioId">Update</a>
									&nbsp;
									<a type="button" class="btn btn-inverse-danger btn-fw" href="/AdminPortfolio/Delete/@item.PortfolioId">Delete</a>
								</td>
							</tr>
						}

					</tbody>
				</table>
			</div>
		</div>
	</div>
</div>

